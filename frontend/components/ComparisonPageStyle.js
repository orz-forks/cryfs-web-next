import { StyleSheet } from 'aphrodite/no-important'

const style = StyleSheet.create({
    image: {
        maxWidth: '100%',
        marginTop: '5px',
        marginBottom: '30px',
        width: '100%',
    },
})

export default style
